-- deob by massive 

local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")

local player = Players.LocalPlayer

-- ========== UTILITIES ==========
local function getPlayerAvatar(userId)
    return "rbxthumb://type=AvatarHeadShot&id=" .. userId .. "&w=150&h=150"
end

local function formatValue(val)
    -- Convert number to short string (e.g., 1.3B, 500M)
    if val >= 1e9 then return string.format("%.1fB", val / 1e9)
    elseif val >= 1e6 then return string.format("%.1fM", val / 1e6)
    elseif val >= 1e3 then return string.format("%.1fK", val / 1e3)
    else return tostring(val) end
end

-- ========== CREATE MAIN GUI ==========
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "VyseDuels"
screenGui.ResetOnSpawn = false
screenGui.Parent = CoreGui

-- Toggle button (main menu opener)
local toggleButton = Instance.new("TextButton")
toggleButton.Size = UDim2.new(0, 164, 0, 36)
toggleButton.Position = UDim2.new(0.5, -82, 0, 12)
toggleButton.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
toggleButton.Text = ""
toggleButton.AutoButtonColor = false
toggleButton.Visible = true
toggleButton.Parent = screenGui

local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(0, 18)
toggleCorner.Parent = toggleButton

local toggleStroke = Instance.new("UIStroke")
toggleStroke.Color = Color3.fromRGB(38, 38, 38)
toggleStroke.Thickness = 1
toggleStroke.Parent = toggleButton

local toggleIcon = Instance.new("ImageLabel")
toggleIcon.Image = "rbxassetid://120042443836261"
toggleIcon.Size = UDim2.new(0, 22, 0, 22)
toggleIcon.Position = UDim2.new(0, 9, 0.5, -11)
toggleIcon.BackgroundTransparency = 1
toggleIcon.ScaleType = Enum.ScaleType.Fit
toggleIcon.Parent = toggleButton

local toggleText = Instance.new("TextLabel")
toggleText.Text = "Vyse Duels"
toggleText.Font = Enum.Font.GothamBold
toggleText.TextSize = 11
toggleText.TextColor3 = Color3.fromRGB(160, 160, 160)
toggleText.Size = UDim2.new(1, -38, 1, 0)
toggleText.Position = UDim2.new(0, 36, 0, 0)
toggleText.BackgroundTransparency = 1
toggleText.TextXAlignment = Enum.TextXAlignment.Left
toggleText.Parent = toggleButton

-- Main frame (hidden initially)
local mainFrame = Instance.new("Frame")
mainFrame.Name = "Main"
mainFrame.Size = UDim2.new(0, 860, 0, 560)
mainFrame.Position = UDim2.new(0.5, -430, 0.5, -280)
mainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
mainFrame.BorderSizePixel = 0
mainFrame.ClipsDescendants = true
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Visible = false
mainFrame.Parent = screenGui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 14)
mainCorner.Parent = mainFrame

-- Left sidebar
local sideBar = Instance.new("Frame")
sideBar.Size = UDim2.new(0, 82, 1, 0)
sideBar.BackgroundColor3 = Color3.fromRGB(16, 16, 16)
sideBar.Parent = mainFrame

local logoFrame = Instance.new("Frame")
logoFrame.Size = UDim2.new(0, 46, 0, 46)
logoFrame.Position = UDim2.new(0.5, -23, 0, 16)
logoFrame.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
logoFrame.Parent = sideBar
local logoCorner = Instance.new("UICorner")
logoCorner.CornerRadius = UDim.new(0, 12)
logoCorner.Parent = logoFrame
local logoStroke = Instance.new("UIStroke")
logoStroke.Color = Color3.fromRGB(55, 55, 55)
logoStroke.Thickness = 1.5
logoStroke.Parent = logoFrame

local logoIcon = Instance.new("ImageLabel")
logoIcon.Size = UDim2.new(1, -6, 1, -6)
logoIcon.Position = UDim2.new(0, 3, 0, 3)
logoIcon.Image = "rbxassetid://120042443836261"
logoIcon.BackgroundTransparency = 1
logoIcon.Parent = logoFrame

local separator = Instance.new("Frame")
separator.Size = UDim2.new(0, 36, 0, 1)
separator.Position = UDim2.new(0.5, -18, 0, 70)
separator.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
separator.Parent = sideBar

-- Navigation buttons
local offersBtn = Instance.new("TextButton")
offersBtn.Name = "Offers"
offersBtn.Size = UDim2.new(1, -12, 0, 62)
offersBtn.Position = UDim2.new(0, 6, 0, 80)
offersBtn.BackgroundTransparency = 1
offersBtn.ZIndex = 2
offersBtn.Parent = sideBar
local offersCorner = Instance.new("UICorner")
offersCorner.CornerRadius = UDim.new(0, 9)
offersCorner.Parent = offersBtn
local offersBg = Instance.new("Frame")
offersBg.Name = "Bg"
offersBg.Size = UDim2.new(1, 0, 1, 0)
offersBg.BackgroundColor3 = Color3.fromRGB(75, 75, 75)
offersBg.BackgroundTransparency = 0.7
offersBg.ZIndex = 1
offersBg.Visible = false
offersBg.Parent = offersBtn
local offersPip = Instance.new("Frame")
offersPip.Name = "Pip"
offersPip.Size = UDim2.new(0, 2, 0, 28)
offersPip.Position = UDim2.new(0, 0, 0.5, -14)
offersPip.BackgroundColor3 = Color3.fromRGB(100, 200, 100)
offersPip.Visible = false
offersPip.Parent = offersBtn
local offersIcon = Instance.new("TextLabel")
offersIcon.Text = "📋"
offersIcon.TextSize = 20
offersIcon.Size = UDim2.new(1, 0, 0, 28)
offersIcon.Position = UDim2.new(0, 0, 0, 5)
offersIcon.BackgroundTransparency = 1
offersIcon.ZIndex = 3
offersIcon.Parent = offersBtn
local offersLabel = Instance.new("TextLabel")
offersLabel.Text = "Offers"
offersLabel.TextSize = 11
offersLabel.Size = UDim2.new(1, 0, 0, 14)
offersLabel.Position = UDim2.new(0, 0, 0, 36)
offersLabel.BackgroundTransparency = 1
offersLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
offersLabel.Font = Enum.Font.GothamBold
offersLabel.Parent = offersBtn

local createBtn = Instance.new("TextButton")
createBtn.Name = "Create"
createBtn.Size = UDim2.new(1, -12, 0, 62)
createBtn.Position = UDim2.new(0, 6, 0, 148)
createBtn.BackgroundTransparency = 1
createBtn.ZIndex = 2
createBtn.Parent = sideBar
local createCorner = Instance.new("UICorner")
createCorner.CornerRadius = UDim.new(0, 9)
createCorner.Parent = createBtn
local createBg = Instance.new("Frame")
createBg.Name = "Bg"
createBg.Size = UDim2.new(1, 0, 1, 0)
createBg.BackgroundColor3 = Color3.fromRGB(75, 75, 75)
createBg.BackgroundTransparency = 0.7
createBg.ZIndex = 1
createBg.Visible = false
createBg.Parent = createBtn
local createPip = Instance.new("Frame")
createPip.Name = "Pip"
createPip.Size = UDim2.new(0, 2, 0, 28)
createPip.Position = UDim2.new(0, 0, 0.5, -14)
createPip.BackgroundColor3 = Color3.fromRGB(100, 200, 100)
createPip.Visible = false
createPip.Parent = createBtn
local createIcon = Instance.new("TextLabel")
createIcon.Text = "✚"
createIcon.TextSize = 20
createIcon.Size = UDim2.new(1, 0, 0, 28)
createIcon.Position = UDim2.new(0, 0, 0, 5)
createIcon.BackgroundTransparency = 1
createIcon.ZIndex = 3
createIcon.Parent = createBtn
local createLabel = Instance.new("TextLabel")
createLabel.Text = "Post"
createLabel.TextSize = 11
createLabel.Size = UDim2.new(1, 0, 0, 14)
createLabel.Position = UDim2.new(0, 0, 0, 36)
createLabel.BackgroundTransparency = 1
createLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
createLabel.Font = Enum.Font.GothamBold
createLabel.Parent = createBtn

local inboxBtn = Instance.new("TextButton")
inboxBtn.Name = "Incoming"
inboxBtn.Size = UDim2.new(1, -12, 0, 62)
inboxBtn.Position = UDim2.new(0, 6, 0, 216)
inboxBtn.BackgroundTransparency = 1
inboxBtn.ZIndex = 2
inboxBtn.Parent = sideBar
local inboxCorner = Instance.new("UICorner")
inboxCorner.CornerRadius = UDim.new(0, 9)
inboxCorner.Parent = inboxBtn
local inboxBg = Instance.new("Frame")
inboxBg.Name = "Bg"
inboxBg.Size = UDim2.new(1, 0, 1, 0)
inboxBg.BackgroundColor3 = Color3.fromRGB(75, 75, 75)
inboxBg.BackgroundTransparency = 0.7
inboxBg.ZIndex = 1
inboxBg.Visible = false
inboxBg.Parent = inboxBtn
local inboxPip = Instance.new("Frame")
inboxPip.Name = "Pip"
inboxPip.Size = UDim2.new(0, 2, 0, 28)
inboxPip.Position = UDim2.new(0, 0, 0.5, -14)
inboxPip.BackgroundColor3 = Color3.fromRGB(100, 200, 100)
inboxPip.Visible = false
inboxPip.Parent = inboxBtn
local inboxIcon = Instance.new("TextLabel")
inboxIcon.Text = "📩"
inboxIcon.TextSize = 20
inboxIcon.Size = UDim2.new(1, 0, 0, 28)
inboxIcon.Position = UDim2.new(0, 0, 0, 5)
inboxIcon.BackgroundTransparency = 1
inboxIcon.ZIndex = 3
inboxIcon.Parent = inboxBtn
local inboxLabel = Instance.new("TextLabel")
inboxLabel.Text = "Inbox"
inboxLabel.TextSize = 11
inboxLabel.Size = UDim2.new(1, 0, 0, 14)
inboxLabel.Position = UDim2.new(0, 0, 0, 36)
inboxLabel.BackgroundTransparency = 1
inboxLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
inboxLabel.Font = Enum.Font.GothamBold
inboxLabel.Parent = inboxBtn

-- Minimize button (bottom)
local minButton = Instance.new("TextButton")
minButton.Size = UDim2.new(1, -12, 0, 38)
minButton.Position = UDim2.new(0, 6, 1, -46)
minButton.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
minButton.BackgroundTransparency = 0.4
minButton.Text = "─"
minButton.TextColor3 = Color3.fromRGB(85, 85, 85)
minButton.Font = Enum.Font.GothamBold
minButton.TextSize = 14
minButton.Parent = sideBar
local minCorner = Instance.new("UICorner")
minCorner.CornerRadius = UDim.new(0, 9)
minCorner.Parent = minButton
local minStroke = Instance.new("UIStroke")
minStroke.Color = Color3.fromRGB(55, 55, 55)
minStroke.Thickness = 1
minStroke.Parent = minButton

-- Right content area
local contentArea = Instance.new("Frame")
contentArea.Size = UDim2.new(1, -82, 1, 0)
contentArea.Position = UDim2.new(0, 82, 0, 0)
contentArea.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
contentArea.Parent = mainFrame

local topBar = Instance.new("Frame")
topBar.Size = UDim2.new(1, 0, 0, 60)
topBar.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
topBar.Parent = contentArea

local pageTitle = Instance.new("TextLabel")
pageTitle.Text = "Live Offers"
pageTitle.TextColor3 = Color3.fromRGB(240, 240, 240)
pageTitle.Size = UDim2.new(0, 260, 1, 0)
pageTitle.Position = UDim2.new(0, 20, 0, 0)
pageTitle.BackgroundTransparency = 1
pageTitle.Font = Enum.Font.GothamBold
pageTitle.TextSize = 16
pageTitle.TextXAlignment = Enum.TextXAlignment.Left
pageTitle.Parent = topBar

local subtitle = Instance.new("TextLabel")
subtitle.Text = "Vyse Duels Finder"
subtitle.TextColor3 = Color3.fromRGB(50, 50, 50)
subtitle.Font = Enum.Font.GothamMedium
subtitle.Size = UDim2.new(0, 130, 0, 14)
subtitle.Position = UDim2.new(0, 20, 1, -16)
subtitle.BackgroundTransparency = 1
subtitle.TextSize = 11
subtitle.Parent = topBar

-- Status blocks
local statusFrame = Instance.new("Frame")
statusFrame.Size = UDim2.new(0, 108, 0, 30)
statusFrame.Position = UDim2.new(1, -306, 0.5, -15)
statusFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
statusFrame.Parent = topBar
local statusCorner = Instance.new("UICorner")
statusCorner.CornerRadius = UDim.new(0, 8)
statusCorner.Parent = statusFrame

local statusDot = Instance.new("Frame")
statusDot.Size = UDim2.new(0, 7, 0, 7)
statusDot.Position = UDim2.new(0, 10, 0.5, -3)
statusDot.BackgroundColor3 = Color3.fromRGB(210, 80, 80)
statusDot.Parent = statusFrame
local dotCorner = Instance.new("UICorner")
dotCorner.CornerRadius = UDim.new(1, 0)
dotCorner.Parent = statusDot

local statusText = Instance.new("TextLabel")
statusText.Text = "Offline"
statusText.Size = UDim2.new(1, -25, 1, 0)
statusText.Position = UDim2.new(0, 24, 0, 0)
statusText.BackgroundTransparency = 1
statusText.TextColor3 = Color3.fromRGB(200, 200, 200)
statusText.Font = Enum.Font.GothamBold
statusText.TextSize = 11
statusText.TextXAlignment = Enum.TextXAlignment.Left
statusText.Parent = statusFrame

local pingFrame = Instance.new("Frame")
pingFrame.Size = UDim2.new(0, 90, 0, 30)
pingFrame.Position = UDim2.new(1, -188, 0.5, -15)
pingFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
pingFrame.Parent = topBar
local pingCorner = Instance.new("UICorner")
pingCorner.CornerRadius = UDim.new(0, 8)
pingCorner.Parent = pingFrame

local pingIcon = Instance.new("TextLabel")
pingIcon.Text = "📡"
pingIcon.Size = UDim2.new(0, 26, 1, 0)
pingIcon.Position = UDim2.new(0, 5, 0, 0)
pingIcon.BackgroundTransparency = 1
pingIcon.Font = Enum.Font.GothamBold
pingIcon.TextSize = 14
pingIcon.Parent = pingFrame

local pingText = Instance.new("TextLabel")
pingText.Text = "-- ms"
pingText.Size = UDim2.new(1, -30, 1, 0)
pingText.Position = UDim2.new(0, 28, 0, 0)
pingText.BackgroundTransparency = 1
pingText.TextColor3 = Color3.fromRGB(200, 200, 200)
pingText.Font = Enum.Font.GothamBold
pingText.TextSize = 11
pingText.TextXAlignment = Enum.TextXAlignment.Left
pingText.Parent = pingFrame

local userFrame = Instance.new("Frame")
userFrame.Size = UDim2.new(0, 88, 0, 30)
userFrame.Position = UDim2.new(1, -88, 0.5, -15)
userFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
userFrame.Parent = topBar
local userCorner = Instance.new("UICorner")
userCorner.CornerRadius = UDim.new(0, 8)
userCorner.Parent = userFrame

local userName = Instance.new("TextLabel")
userName.Text = player.DisplayName
userName.Size = UDim2.new(1, -8, 1, 0)
userName.Position = UDim2.new(0, 4, 0, 0)
userName.BackgroundTransparency = 1
userName.TextColor3 = Color3.fromRGB(210, 210, 210)
userName.Font = Enum.Font.GothamBold
userName.TextSize = 11
userName.TextXAlignment = Enum.TextXAlignment.Center
userName.TextTruncate = Enum.TextTruncate.AtEnd
userName.Parent = userFrame

-- Search/filter bar
local filterFrame = Instance.new("Frame")
filterFrame.Size = UDim2.new(1, -28, 0, 36)
filterFrame.Position = UDim2.new(0, 14, 0, 70)
filterFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
filterFrame.Parent = contentArea
local filterCorner = Instance.new("UICorner")
filterCorner.CornerRadius = UDim.new(0, 10)
filterCorner.Parent = filterFrame

local searchIcon = Instance.new("TextLabel")
searchIcon.Text = "🔍"
searchIcon.Size = UDim2.new(0, 36, 1, 0)
searchIcon.BackgroundTransparency = 1
searchIcon.Font = Enum.Font.GothamBold
searchIcon.TextSize = 13
searchIcon.Parent = filterFrame

local searchBox = Instance.new("TextBox")
searchBox.Size = UDim2.new(1, -44, 1, 0)
searchBox.Position = UDim2.new(0, 38, 0, 0)
searchBox.PlaceholderText = "Search player or item..."
searchBox.PlaceholderColor3 = Color3.fromRGB(85, 85, 85)
searchBox.Text = ""
searchBox.BackgroundTransparency = 1
searchBox.TextColor3 = Color3.fromRGB(240, 240, 240)
searchBox.Font = Enum.Font.GothamMedium
searchBox.TextSize = 13
searchBox.TextXAlignment = Enum.TextXAlignment.Left
searchBox.ClearTextOnFocus = false
searchBox.Parent = filterFrame

-- Value filters
local valueFilterFrame = Instance.new("Frame")
valueFilterFrame.Size = UDim2.new(1, -28, 0, 70)
valueFilterFrame.Position = UDim2.new(0, 14, 0, 112)
valueFilterFrame.BackgroundColor3 = Color3.fromRGB(14, 14, 14)
valueFilterFrame.Parent = contentArea
local valueCorner = Instance.new("UICorner")
valueCorner.CornerRadius = UDim.new(0, 10)
valueCorner.Parent = valueFilterFrame

local minLabel = Instance.new("TextLabel")
minLabel.Text = "Min Value"
minLabel.Size = UDim2.new(0, 130, 0, 14)
minLabel.Position = UDim2.new(0, 12, 0, 6)
minLabel.BackgroundTransparency = 1
minLabel.TextColor3 = Color3.fromRGB(160, 160, 160)
minLabel.Font = Enum.Font.GothamBold
minLabel.TextSize = 11
minLabel.Parent = valueFilterFrame

local minFrame = Instance.new("Frame")
minFrame.Size = UDim2.new(0, 130, 0, 26)
minFrame.Position = UDim2.new(0, 12, 0, 22)
minFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
minFrame.Parent = valueFilterFrame
local minFrameCorner = Instance.new("UICorner")
minFrameCorner.CornerRadius = UDim.new(0, 7)
minFrameCorner.Parent = minFrame

local minBox = Instance.new("TextBox")
minBox.Size = UDim2.new(1, -10, 1, 0)
minBox.Position = UDim2.new(0, 6, 0, 0)
minBox.PlaceholderText = "e.g. 100m"
minBox.BackgroundTransparency = 1
minBox.TextColor3 = Color3.fromRGB(240, 240, 240)
minBox.Font = Enum.Font.GothamMedium
minBox.TextSize = 11
minBox.ClearTextOnFocus = false
minBox.Parent = minFrame

local maxLabel = Instance.new("TextLabel")
maxLabel.Text = "Max Value"
maxLabel.Size = UDim2.new(0, 130, 0, 14)
maxLabel.Position = UDim2.new(0, 158, 0, 6)
maxLabel.BackgroundTransparency = 1
maxLabel.TextColor3 = Color3.fromRGB(160, 160, 160)
maxLabel.Font = Enum.Font.GothamBold
maxLabel.TextSize = 11
maxLabel.Parent = valueFilterFrame

local maxFrame = Instance.new("Frame")
maxFrame.Size = UDim2.new(0, 130, 0, 26)
maxFrame.Position = UDim2.new(0, 158, 0, 22)
maxFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
maxFrame.Parent = valueFilterFrame
local maxFrameCorner = Instance.new("UICorner")
maxFrameCorner.CornerRadius = UDim.new(0, 7)
maxFrameCorner.Parent = maxFrame

local maxBox = Instance.new("TextBox")
maxBox.Size = UDim2.new(1, -10, 1, 0)
maxBox.Position = UDim2.new(0, 6, 0, 0)
maxBox.PlaceholderText = "e.g. 1b"
maxBox.BackgroundTransparency = 1
maxBox.TextColor3 = Color3.fromRGB(240, 240, 240)
maxBox.Font = Enum.Font.GothamMedium
maxBox.TextSize = 11
maxBox.ClearTextOnFocus = false
maxBox.Parent = maxFrame

-- Offers list (ScrollingFrame)
local offersScroll = Instance.new("ScrollingFrame")
offersScroll.Size = UDim2.new(1, -28, 1, -194)
offersScroll.Position = UDim2.new(0, 14, 0, 190)
offersScroll.BackgroundTransparency = 1
offersScroll.ScrollBarThickness = 2
offersScroll.ScrollBarImageColor3 = Color3.fromRGB(55, 55, 55)
offersScroll.BorderSizePixel = 0
offersScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
offersScroll.Visible = true
offersScroll.Parent = contentArea

local offersList = Instance.new("UIListLayout")
offersList.Parent = offersScroll
offersList.Padding = UDim.new(0, 5)
offersList.SortOrder = Enum.SortOrder.LayoutOrder

offersList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    offersScroll.CanvasSize = UDim2.new(0, 0, 0, offersList.AbsoluteContentSize.Y + 10)
end)

-- Empty state label
local emptyState = Instance.new("TextLabel")
emptyState.Name = "EmptyState"
emptyState.Text = "No offers yet\nWaiting for duels..."
emptyState.Size = UDim2.new(1, 0, 0, 80)
emptyState.LayoutOrder = 999999
emptyState.BackgroundTransparency = 1
emptyState.TextColor3 = Color3.fromRGB(100, 100, 100)
emptyState.Font = Enum.Font.GothamMedium
emptyState.TextSize = 14
emptyState.TextXAlignment = Enum.TextXAlignment.Center
emptyState.TextYAlignment = Enum.TextYAlignment.Center
emptyState.Visible = true
emptyState.Parent = offersScroll

-- Create content for each page (Offers, Create, Inbox)
-- We'll implement dynamic switching

-- Currently selected pet (for posting)
local selectedPet = nil

-- Pet selection popup
local petPopup = Instance.new("Frame")
petPopup.Name = "PetPopup"
petPopup.Size = UDim2.new(0, 520, 0, 440)
petPopup.Position = UDim2.new(0.5, -260, 0.5, -220)
petPopup.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
petPopup.BackgroundTransparency = 0.05
petPopup.ZIndex = 50
petPopup.Visible = false
petPopup.Parent = screenGui
local popupCorner = Instance.new("UICorner")
popupCorner.CornerRadius = UDim.new(0, 12)
popupCorner.Parent = petPopup
local popupStroke = Instance.new("UIStroke")
popupStroke.Thickness = 1.5
popupStroke.Color = Color3.fromRGB(40, 40, 40)
popupStroke.Parent = petPopup

local popupTitle = Instance.new("TextLabel")
popupTitle.Text = "Select Pet To Post"
popupTitle.TextSize = 16
popupTitle.Size = UDim2.new(1, -120, 0, 48)
popupTitle.Position = UDim2.new(0, 20, 0, 0)
popupTitle.BackgroundTransparency = 1
popupTitle.TextColor3 = Color3.fromRGB(240, 240, 240)
popupTitle.Font = Enum.Font.GothamBold
popupTitle.TextXAlignment = Enum.TextXAlignment.Left
popupTitle.Parent = petPopup

local closePopup = Instance.new("TextButton")
closePopup.Text = "X"
closePopup.Size = UDim2.new(0, 32, 0, 32)
closePopup.Position = UDim2.new(1, -44, 0, 8)
closePopup.BackgroundTransparency = 1
closePopup.TextColor3 = Color3.fromRGB(160, 160, 160)
closePopup.TextSize = 12
closePopup.Font = Enum.Font.GothamBold
closePopup.Parent = petPopup
closePopup.MouseButton1Click:Connect(function()
    petPopup.Visible = false
end)

local refreshPets = Instance.new("TextButton")
refreshPets.Text = "🔄 Refresh"
refreshPets.Size = UDim2.new(0, 88, 0, 30)
refreshPets.Position = UDim2.new(1, -148, 0, 10)
refreshPets.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
refreshPets.TextColor3 = Color3.fromRGB(130, 130, 130)
refreshPets.Font = Enum.Font.GothamBold
refreshPets.TextSize = 11
refreshPets.Parent = petPopup
local refreshCorner = Instance.new("UICorner")
refreshCorner.CornerRadius = UDim.new(0, 8)
refreshCorner.Parent = refreshPets

local petScroll = Instance.new("ScrollingFrame")
petScroll.Size = UDim2.new(1, -20, 1, -62)
petScroll.Position = UDim2.new(0, 10, 0, 54)
petScroll.BackgroundTransparency = 1
petScroll.ScrollBarThickness = 3
petScroll.ZIndex = 51
petScroll.Parent = petPopup

local petList = Instance.new("UIListLayout")
petList.Parent = petScroll
petList.Padding = UDim.new(0, 5)

local noPetsLabel = Instance.new("TextLabel")
noPetsLabel.Text = "No pets found on your plot.\nMake sure you have animals placed!"
noPetsLabel.Size = UDim2.new(1, 0, 0, 80)
noPetsLabel.BackgroundTransparency = 1
noPetsLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
noPetsLabel.Font = Enum.Font.GothamMedium
noPetsLabel.TextSize = 12
noPetsLabel.TextXAlignment = Enum.TextXAlignment.Center
noPetsLabel.TextYAlignment = Enum.TextYAlignment.Center
noPetsLabel.LayoutOrder = 999
noPetsLabel.Parent = petScroll

-- Function to load pets from the player's plot (simulate - in real game you'd scan workspace)
local function loadPets()
    -- Clear existing pet buttons
    for _, child in pairs(petScroll:GetChildren()) do
        if child:IsA("TextButton") then child:Destroy() end
    end
    -- Placeholder: add some dummy pets for demonstration
    local pets = {"Capitano Moby", "Nuclearo Dinossauro", "Ketupat Kepat", "Garama and Madundung", "Tang Tang Keletang"}
    for _, petName in ipairs(pets) do
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(1, -10, 0, 40)
        btn.Position = UDim2.new(0, 5, 0, 0)
        btn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
        btn.Text = petName
        btn.TextColor3 = Color3.fromRGB(220, 220, 220)
        btn.Font = Enum.Font.GothamMedium
        btn.TextSize = 12
        btn.Parent = petScroll
        local btnCorner = Instance.new("UICorner")
        btnCorner.CornerRadius = UDim.new(0, 6)
        btnCorner.Parent = btn
        btn.MouseButton1Click:Connect(function()
            selectedPet = petName
            petPopup.Visible = false
            -- Update UI to show selected pet
            local petDisplay = contentArea:FindFirstChild("SelectedPetLabel")
            if petDisplay then petDisplay.Text = "Selected: " .. petName end
        end)
    end
    noPetsLabel.Visible = (#pets == 0)
end

refreshPets.MouseButton1Click:Connect(loadPets)

-- Create the "Create Listing" page content
local createPage = nil
local function showCreatePage()
    pageTitle.Text = "Create Listing"
    -- Clear offersScroll and show the creation UI instead
    for _, child in pairs(offersScroll:GetChildren()) do
        if child ~= offersList and child ~= emptyState then child:Destroy() end
    end
    emptyState.Visible = false
    
    -- Create create listing frame
    local createFrame = Instance.new("Frame")
    createFrame.Size = UDim2.new(1, 0, 0, 280)
    createFrame.BackgroundColor3 = Color3.fromRGB(14, 14, 14)
    createFrame.Parent = offersScroll
    local frameCorner = Instance.new("UICorner")
    frameCorner.CornerRadius = UDim.new(0, 11)
    frameCorner.Parent = createFrame
    
    local selectedPetLabel = Instance.new("TextLabel")
    selectedPetLabel.Name = "SelectedPetLabel"
    selectedPetLabel.Text = selectedPet and ("Selected: " .. selectedPet) or "No pet selected"
    selectedPetLabel.TextSize = 14
    selectedPetLabel.Size = UDim2.new(0, 340, 0, 24)
    selectedPetLabel.Position = UDim2.new(0, 16, 0, 8)
    selectedPetLabel.BackgroundTransparency = 1
    selectedPetLabel.TextColor3 = Color3.fromRGB(160, 210, 140)
    selectedPetLabel.Font = Enum.Font.GothamBold
    selectedPetLabel.TextXAlignment = Enum.TextXAlignment.Left
    selectedPetLabel.Parent = createFrame
    
    local selectPetBtn = Instance.new("TextButton")
    selectPetBtn.Text = "SELECT PET"
    selectPetBtn.Size = UDim2.new(0, 110, 0, 32)
    selectPetBtn.Position = UDim2.new(1, -118, 0.5, -16)
    selectPetBtn.BackgroundColor3 = Color3.fromRGB(75, 75, 75)
    selectPetBtn.TextColor3 = Color3.fromRGB(240, 240, 240)
    selectPetBtn.Font = Enum.Font.GothamBold
    selectPetBtn.TextSize = 11
    selectPetBtn.Parent = createFrame
    local selectBtnCorner = Instance.new("UICorner")
    selectBtnCorner.CornerRadius = UDim.new(0, 6)
    selectBtnCorner.Parent = selectPetBtn
    selectPetBtn.MouseButton1Click:Connect(function()
        loadPets()
        petPopup.Visible = true
    end)
    
    local minOfferLabel = Instance.new("TextLabel")
    minOfferLabel.Text = "Min Offer Value"
    minOfferLabel.Size = UDim2.new(0.5, 0, 0, 14)
    minOfferLabel.Position = UDim2.new(0, 0, 0, 78)
    minOfferLabel.BackgroundTransparency = 1
    minOfferLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
    minOfferLabel.Font = Enum.Font.GothamBold
    minOfferLabel.TextSize = 11
    minOfferLabel.Parent = createFrame
    
    local minOfferFrame = Instance.new("Frame")
    minOfferFrame.Size = UDim2.new(0.5, -5, 0, 43)
    minOfferFrame.Position = UDim2.new(0, 0, 0, 95)
    minOfferFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    minOfferFrame.Parent = createFrame
    local minOfferCorner = Instance.new("UICorner")
    minOfferCorner.CornerRadius = UDim.new(0, 8)
    minOfferCorner.Parent = minOfferFrame
    
    local minOfferBox = Instance.new("TextBox")
    minOfferBox.Size = UDim2.new(1, -20, 1, 0)
    minOfferBox.Position = UDim2.new(0, 12, 0, 0)
    minOfferBox.PlaceholderText = "e.g. 10m, 500k, 1b"
    minOfferBox.BackgroundTransparency = 1
    minOfferBox.TextColor3 = Color3.fromRGB(240, 240, 240)
    minOfferBox.Font = Enum.Font.GothamMedium
    minOfferBox.TextSize = 11
    minOfferBox.ClearTextOnFocus = false
    minOfferBox.Parent = minOfferFrame
    
    local infoLabel = Instance.new("TextLabel")
    infoLabel.Text = "ℹ Incoming offers below this value will be hidden in Inbox"
    infoLabel.Size = UDim2.new(1, 0, 0, 14)
    infoLabel.Position = UDim2.new(0, 0, 0, 147)
    infoLabel.BackgroundTransparency = 1
    infoLabel.TextColor3 = Color3.fromRGB(130, 130, 130)
    infoLabel.Font = Enum.Font.Gotham
    infoLabel.TextSize = 10
    infoLabel.Parent = createFrame
    
    local postBtn = Instance.new("TextButton")
    postBtn.Text = "📤 POST OFFER"
    postBtn.Size = UDim2.new(1, 0, 0, 52)
    postBtn.Position = UDim2.new(0, 0, 0, 180)
    postBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    postBtn.TextColor3 = Color3.fromRGB(240, 240, 240)
    postBtn.Font = Enum.Font.GothamBold
    postBtn.TextSize = 12
    postBtn.Parent = createFrame
    local postCorner = Instance.new("UICorner")
    postCorner.CornerRadius = UDim.new(0, 8)
    postCorner.Parent = postBtn
    postBtn.MouseButton1Click:Connect(function()
        if not selectedPet then
            -- show notification: select pet first
            return
        end
        local minVal = minOfferBox.Text
        -- Here you would send the offer to the server via remote event
        print("Posting offer:", selectedPet, "min:", minVal)
    end)
end

-- Inbox page (incoming offers)
local function showInboxPage()
    pageTitle.Text = "Inbox"
    -- Clear and show incoming offers (placeholder)
    for _, child in pairs(offersScroll:GetChildren()) do
        if child ~= offersList and child ~= emptyState then child:Destroy() end
    end
    emptyState.Visible = false
    
    local inboxFrame = Instance.new("Frame")
    inboxFrame.Size = UDim2.new(1, 0, 0, 100)
    inboxFrame.BackgroundColor3 = Color3.fromRGB(14, 14, 14)
    inboxFrame.Parent = offersScroll
    local inboxCorner = Instance.new("UICorner")
    inboxCorner.CornerRadius = UDim.new(0, 11)
    inboxCorner.Parent = inboxFrame
    
    local noIncoming = Instance.new("TextLabel")
    noIncoming.Text = "No incoming offers\nCreate a listing to start receiving"
    noIncoming.Size = UDim2.new(1, 0, 1, 0)
    noIncoming.BackgroundTransparency = 1
    noIncoming.TextColor3 = Color3.fromRGB(120, 120, 120)
    noIncoming.Font = Enum.Font.GothamMedium
    noIncoming.TextSize = 13
    noIncoming.TextXAlignment = Enum.TextXAlignment.Center
    noIncoming.TextYAlignment = Enum.TextYAlignment.Center
    noIncoming.Parent = inboxFrame
end

-- Live offers page (default)
local function showOffersPage()
    pageTitle.Text = "Live Offers"
    -- Clear and show dummy offers (for demo)
    for _, child in pairs(offersScroll:GetChildren()) do
        if child ~= offersList and child ~= emptyState then child:Destroy() end
    end
    emptyState.Visible = false
    
    -- Sample offers (in real script these would come from network)
    local sampleOffers = {
        {player = "arthurfriz", items = "Capitano Moby", value = "1.3B/s", avatar = ""},
        {player = "1KINGDEMONJUNGKOOK1", items = "Nuclearo Dinossauro", value = "60M/s", avatar = ""},
        {player = "reo_sprng", items = "Spaghetti Tualetti", value = "60M/s", avatar = ""},
        {player = "zouzou590002", items = "Cooki and Milki", value = "155M/s", avatar = ""},
    }
    
    for _, off in ipairs(sampleOffers) do
        local offerFrame = Instance.new("Frame")
        offerFrame.Size = UDim2.new(1, 0, 0, 78)
        offerFrame.BackgroundColor3 = Color3.fromRGB(14, 14, 14)
        offerFrame.Parent = offersScroll
        local frameCorner = Instance.new("UICorner")
        frameCorner.CornerRadius = UDim.new(0, 10)
        frameCorner.Parent = offerFrame
        
        -- Avatar placeholder
        local avatarFrame = Instance.new("Frame")
        avatarFrame.Size = UDim2.new(0, 56, 0, 56)
        avatarFrame.Position = UDim2.new(0, 10, 0.5, -28)
        avatarFrame.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
        avatarFrame.Parent = offerFrame
        local avCorner = Instance.new("UICorner")
        avCorner.CornerRadius = UDim.new(0, 12)
        avCorner.Parent = avatarFrame
        
        local playerName = Instance.new("TextLabel")
        playerName.Text = off.player
        playerName.Size = UDim2.new(0, 180, 0, 20)
        playerName.Position = UDim2.new(0, 76, 0, 11)
        playerName.BackgroundTransparency = 1
        playerName.TextColor3 = Color3.fromRGB(240, 240, 240)
        playerName.Font = Enum.Font.GothamBold
        playerName.TextSize = 12
        playerName.TextXAlignment = Enum.TextXAlignment.Left
        playerName.Parent = offerFrame
        
        local itemInfo = Instance.new("TextLabel")
        itemInfo.Text = off.items .. ";-;" .. off.value
        itemInfo.Size = UDim2.new(0, 300, 0, 20)
        itemInfo.Position = UDim2.new(0, 76, 0, 38)
        itemInfo.BackgroundTransparency = 1
        itemInfo.TextColor3 = Color3.fromRGB(180, 180, 180)
        itemInfo.Font = Enum.Font.Gotham
        itemInfo.TextSize = 11
        itemInfo.TextXAlignment = Enum.TextXAlignment.Left
        itemInfo.Parent = offerFrame
        
        local makeOffer = Instance.new("TextButton")
        makeOffer.Text = "Make Offer"
        makeOffer.Size = UDim2.new(0, 100, 0, 30)
        makeOffer.Position = UDim2.new(1, -110, 0.5, -15)
        makeOffer.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
        makeOffer.TextColor3 = Color3.fromRGB(240, 240, 240)
        makeOffer.Font = Enum.Font.GothamBold
        makeOffer.TextSize = 11
        makeOffer.Parent = offerFrame
        local moCorner = Instance.new("UICorner")
        moCorner.CornerRadius = UDim.new(0, 6)
        moCorner.Parent = makeOffer
        makeOffer.MouseButton1Click:Connect(function()
            print("Make offer to", off.player)
        end)
    end
end

-- Navigation logic
offersBtn.MouseButton1Click:Connect(function()
    showOffersPage()
    -- Visual feedback
    offersBg.Visible = true
    createBg.Visible = false
    inboxBg.Visible = false
    offersPip.Visible = true
    createPip.Visible = false
    inboxPip.Visible = false
end)

createBtn.MouseButton1Click:Connect(function()
    showCreatePage()
    offersBg.Visible = false
    createBg.Visible = true
    inboxBg.Visible = false
    offersPip.Visible = false
    createPip.Visible = true
    inboxPip.Visible = false
end)

inboxBtn.MouseButton1Click:Connect(function()
    showInboxPage()
    offersBg.Visible = false
    createBg.Visible = false
    inboxBg.Visible = true
    offersPip.Visible = false
    createPip.Visible = false
    inboxPip.Visible = true
end)

-- Show offers page by default
showOffersPage()
offersBg.Visible = true
offersPip.Visible = true

-- Toggle main frame visibility
local guiVisible = true
toggleButton.MouseButton1Click:Connect(function()
    guiVisible = not guiVisible
    mainFrame.Visible = guiVisible
end)

-- End key toggle
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.End then
        guiVisible = not guiVisible
        mainFrame.Visible = guiVisible
    end
end)

-- Minimize button functionality (collapse sidebar? actually just close main frame)
minButton.MouseButton1Click:Connect(function()
    mainFrame.Visible = false
    guiVisible = false
end)

-- Load pets when popup opens (first time)
loadPets()

print("Vyse Duels v2 loaded (deobfuscated). Press End to toggle GUI.")
